spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  application:
    # nacos 服务名称，尽量用-，不要用_
    name: content-center
  profiles:
    active: dev
